# Audit complet Tik + btc-research-lab — 2026-06-24

> Méthode : 6 auditeurs en parallèle, **lecture seule**, sur le code réel. Chaque finding classé
> CONFIRMÉ / SUSPECT / OK, avec `fichier:ligne`. Doute méthodique : on distingue bug réel, dette,
> et choix intentionnel documenté.

## ⚠️ CAVEAT #1 — L'audit a porté sur la COPIE `escltheo-eng`, PAS sur la prod
Les agents ont lu mes clones `escltheo-eng/Tik` + `escltheo-eng/btc-research-lab`. **Or le VPS déploie
depuis `Lolasiku-prog`**, et on a DÉJÀ constaté que les deux divergent (ex. `core/.../api/signals.py` :
le VPS portait `ADR-00`/`ADR-030` là où la copie portait autre chose). **Donc chaque finding ci-dessous
doit être reconfirmé contre le code réel du VPS avant correction.** C'est le risque structurel n°1.

---

## 1. Findings ACTIONNABLES (par sévérité)

| # | Sévérité | Fichier:ligne | Finding | Statut |
|---|---|---|---|---|
| A | 🟥 MOYENNE+ | `aggregator/fred_ingester.py:143` | `redis.set(...)` **sans `ex=`** → séries macro FRED (taux, récession, liquidité) en cache **indéfini** → valeur **périmée servie comme fraîche** si FRED décroche. Seul ingester sans TTL (les ~16 autres en ont un). | **CONFIRMÉ** (nouveau) |
| B | 🟧 MOYENNE | `aggregator/cryptocompare_ingester.py` + `scripts/run_ingesters.py` | Quota CryptoCompare : **~90/100 req/mois → 10 de marge**. Chaque `restart ingesters` = +1 req. 3 restarts/mois = dépassement → retour « BTC neutral » (Bug 15). | **CONFIRMÉ** (fragile) |
| C | 🟧 MOYENNE | `config.py:32` | `secret_key` défaut `"dev-secret-change-me"` → risque si `.env` prod oublié. | **CONFIRMÉ** |
| D | 🟨 BASSE | `config.py` (global) | Toggle **`TIK_MICRO_ENABLED` absent de `config.py`** (lu en dur via `os.environ` côté pont labo). Dette ADR-033. | **CONFIRMÉ** |
| E | 🟨 BASSE | `storage/models.py:120` | `circuit_breaker_status` = `String(32)` **sans enum** → valeur invalide passerait en base (l'ingest la force à `degraded`, OK ; engines à vérifier). | **SUSPECT** |
| F | 🟨 BASSE | dashboard `src/macro/discipline.ts:37` vs `src/utils/time.ts:14` | Regex timezone **dupliquée** (`parseUtcIso` non réutilisé) → divergence possible si l'une évolue. | **CONFIRMÉ** |
| G | 🟨 BASSE | dashboard `app/signal/[id].tsx:178` | Cast `as any` + page `signal/[id]` **quasi morte** (la nav utilise `signal-cosmique/[id]`). Incohérence/cleanup. | **CONFIRMÉ** |
| H | 🟦 INFO | `storage/schemas.py:147-149` | Endpoint `ingest` accepte `counter_scenarios=[]` → la « paranoïa contrôlée » (≥2 contre-scénarios) **non imposée** sur le micro. | **SUSPECT** (by-design ?) |
| I | 🟦 INFO | migrations (global) | Pas d'index partiel `signals(horizon)` → non bloquant, à voir post-scale. | OK/futur |

## 2. Incohérences CONNUES, intentionnelles (confirmées dans le code)
- **ADR-026 (A1)** `scoring/cross_validator.py:271-280` : l'outlier Fear&Greed est exclu de la direction ET du circuit-breaker, **mais reste dans la dispersion qui calcule la veracity** → `circuit=ok` mais `veracity=0.70` sur le même short. Documenté, recalibration différée (Axe #1 : ne pas gonfler la veracity).
- **ADR-026 (A5)** : estimateur **`pstdev` (N=2) vs `stdev` (N≥3)** mélangés. Documenté, à trancher.
- **DuckDB WAL** (labo `storage.py`) : risque résiduel de corruption si `docker kill -9` **sans** SIGTERM. Mitigé par `atexit`+checkpoint. Edge case.

## 3. Conflits TRANSVERSES (vus de ma synthèse, pas par un agent isolé)
- 🟥 **Divergence des 2 dépôts** (cf. Caveat #1) : `escltheo` (engineering) ≠ `Lolasiku` (prod VPS). Toute correction doit cibler le bon dépôt ; les fixes poussés sur `escltheo` **n'atteignent pas le VPS**.
- 🟧 **Collision ADR-030** : sur `escltheo`, `main` (doublure) appelle la fusion « ADR-030 », alors que `refonte`/prod réserve 030 au régime de risque et la fusion = 033. Commentaires corrigés sur prod, mais la **branche doublure + `main` portent encore la collision** → décision « refonte = officielle » + suppression doublure **pas encore exécutée**.
- 🟧 **Déploiement en attente** : Stage 1 dashboard + `measure_micro` **pas sur le VPS** ; **clé API à régénérer** (fuitée) ; shadow micro **tout juste démarré** (à re-checker dans quelques jours).

## 4. RASSURANT — confirmé FIXÉ / OK (aucune régression)
- WebSocket **Bug 7/10/16** : fixes présents et corrects (import dynamique, `break` client parti, `get_message(timeout=30)` + race subscribe/accept).
- DB **Bug 1-3** (hypertable PK composite, FK composite feedbacks, UUID→String(36)) : alignés modèle↔migration. **Bug 9** (strip tzinfo asyncpg) présent et correct. **Bug 12** (cache track record adaptatif) présent.
- **ADR-018** : indicateurs techniques bien à **poids 0** (direction 100 % OSINT). Overlays `_enrich_with_*` respectent le contrat `[-1,+1]`/None. Pas de division par zéro, pas de confusion None/0.
- **Scheduler** : `max_instances=1`, `coalesce`, gestion d'erreur par job, arrêt gracieux, crons UTC disjoints (anti-collision Ollama).
- **Endpoint `ingest`** : force `horizon=micro` + `circuit_breaker=degraded`, validation Pydantic (direction regex, bornes [0,1]). Pas d'injection de faux swing/flash.
- **Labo** : **aucune fuite de données** (features backward-only, asof `direction=backward`, features `volx_` incluses), train/serve **symétrique** (`build_features_with_joins`), quantile crossing corrigé (isotonic sort), calibration gated (n≥30), pont `tik_bridge` long-only + OFF par défaut, concurrence DuckDB single-writer + lock.

## 5. Ce qui N'A PAS été vérifié (transparence)
- **Aucun test pytest exécuté** ; aucun comportement **runtime** validé.
- **Contrat API↔dashboard pas recoupé endpoint par endpoint** (champs lus vs renvoyés).
- Findings **non reconfirmés contre la prod `Lolasiku`** (Caveat #1).
- Endpoints `polymarket`, `manual_trades` (sensible trading) peu audités.
- Pas de `npm audit` / `EXPLAIN ANALYZE` / vérif perf.

## 6. Ordre de correction recommandé (du plus utile au moins)
1. **A — FRED TTL** (fix trivial `ex=86400`) — évite de la donnée de contexte fausse. *Reconfirmer d'abord sur le VPS.*
2. **B/C — quota CryptoCompare (passer à 12h) + `secret_key` prod** — robustesse/sécurité.
3. **Conflit transverse — décider de la réconciliation des 2 dépôts** (sinon chaque fix risque le mauvais endroit). Puis exécuter « refonte = officielle » + supprimer la doublure.
4. **D/E/F/G** — dettes mineures (toggle config, enum, DRY dashboard, page morte) — au fil de l'eau.
5. **ADR-026** — recalibration veracity : différée tant que pas d'edge (cohérent NO-GO).
