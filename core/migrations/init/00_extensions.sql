-- Activé automatiquement par l'image timescale/timescaledb
-- Ce fichier garantit que l'extension est bien disponible au boot.
CREATE EXTENSION IF NOT EXISTS timescaledb;
CREATE EXTENSION IF NOT EXISTS pg_trgm;
